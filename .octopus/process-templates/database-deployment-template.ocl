name = "Database deployment template"
description = <<-EOT
        ## Standard Database deployment with approval
        
        Standard database deployment with an approval to review the database delta
        EOT

icon {
    color = "#895ED3"
    id = "database"
}